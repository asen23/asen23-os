modules:
  - type: copy
    from: akmod
    src: /var/cache/rpms/kmods/
    dest: /tmp/rpms

  - type: containerfile
    snippets:
      - RUN find /tmp/rpms
      - RUN dnf -y install /tmp/rpms/*nct6687d*.rpm
